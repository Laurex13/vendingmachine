package se.lexicon.laurita;

public class VendingMachine {

    void addCurrency (int amount) {         //Add to the deposit pool (moneyPool).

    }
     Product request (int productNumber) {          //Buy a Product.
       return getDescription();
     }

    private Product getDescription() {

        return getDescription();
    }

    int endSession (){                                 //Should return change and resets the deposit pool.
        return getBalance();
     }
    String getDescription (int productNumber){            // this should view a product description.
        return getDescription(productNumber);

    }
    int getBalance (){                              //this should returns the deposit pool amount.
       return getBalance();
    }
    String [] getProducts (){
      return getProducts();
    }
}
